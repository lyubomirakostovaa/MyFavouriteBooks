%h1 All Books

%table#books
  %thead
    %tr
      %th Book Title
      %th Genre
      %th Publish Date
      %th More Info
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title 
        %td= book.genre
        %td= book.publish_date
        %td= link_to "More about #{book.title}", book_path(book)